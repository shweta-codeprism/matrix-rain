# Matrix rain
Matrix Rain in React for you website.


### Development build
To build it locally, run

```
npm i
npm start
```

then open `http://localhost:3000/` in the browser.

###  Production build

```
npm i
npm run build
npm run serve
```

